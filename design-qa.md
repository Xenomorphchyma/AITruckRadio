# Design QA — панель AI Truck Radio

Статус: **PASS**
Дата: 2026-07-13

## Сравнение с выбранным референсом

- Референс: `C:\Users\Никита\.codex\generated_images\019f59e4-76e5-7ec2-88f9-985a8ffe6bf6\exec-039e09ba-96ea-4217-992c-4125b0f5ccfa.png`
- Реализация: `C:\Users\Никита\.codex\visualizations\2026\07\13\019f59e4-76e5-7ec2-88f9-985a8ffe6bf6\panel-v2-final-1487x1058.png`
- Совмещённое сравнение: `C:\Users\Никита\.codex\visualizations\2026\07\13\019f59e4-76e5-7ec2-88f9-985a8ffe6bf6\design-qa-reference-vs-implementation.png`
- Повторное сравнение плана после UX-правок: `C:\Users\Никита\.codex\visualizations\2026\07\13\019f59e4-76e5-7ec2-88f9-985a8ffe6bf6\plan-reference-vs-final.png`.
- Одинаковый viewport: **1487 × 1058**.
- Одинаковое состояние: остановленный эфир, заполненный план, выбранная реплика ведущих.

Сохранены композиция, светлая визуальная система, боковая навигация, трёхколоночный план, синие акценты и сквозной нижний плеер. Осознанные исправления референса: активен фактический раздел «План»; в выбранном элементе показан полный текст; справа добавлена готовность перед запуском; в плеере явно написано «Радио остановлено». Drag-handle и меню строк теперь не декорации: они перемещают и изменяют будущие блоки.

## Проверенные состояния и действия

- Все восемь разделов навигации, hash-маршруты и активные состояния, включая редакторскую ленту новостей.
- План: выбор строки, текст и ведущие, сохранение/переозвучивание, drawer параметров, popover действий, закрытие снаружи и по Escape.
- Подготовка плана теперь отменяется из верхней кнопки и из прогресс-блока; отмена останавливает публикацию нового результата и сохраняет прежний готовый план.
- Перетаскивание использует полный снимок строки вместо одного drag-handle; список раздвигается под placeholder той же высоты, а место сброса отмечено пунктирным контуром.
- Исходная строка на время drag больше не занимает место в потоке: соседний блок сразу поднимается, поэтому контур показывает фактическую итоговую позицию, а не место «под самим собой».
- Статус готового блока сокращён до единого «Готово» в плане и инспекторе.
- Действия, зависящие от OmniVoice и LM Studio, заранее помечаются предупреждением и перед продолжением показывают конкретно отсутствующий сервис.
- Polling не стирает несохранённый черновик текста.
- Сыгранная и текущая реплики read-only; будущая реплика редактируется. «Далее» начинается с `show_plan_next_index`.
- Остановленный и активный эфир; readiness видна до старта и анимированно уезжает вправо после старта, включая мобильный режим.
- Плеер: полноширинный dock, play/skip/back/volume, прямая стрелка без лишнего меню и реальное сворачивание в центральную капсулу 430×50 пикселей.
- Настройки: поиск, details-группы, переключатели, системные вкладки, добавление/удаление ведущего и reference-upload.
- Все переключатели pill-формы; Reference ASR выведен одной горизонтальной строкой. Сброс вынесен в отдельную компактную кнопку, скрыт для неизменённых значений и недоступен по клику на подпись.
- Профили настроек: создание, применение, переименование и удаление с подтверждением; секреты, локальные пути и reference-голоса в профиль не попадают.
- Reference voice: проверка WAV и текста, сравнение ручной расшифровки с ASR, предупреждения о тишине/языке/низкой уверенности и блокировка явно плохого референса перед клонированием.
- Новости: свежесть ленты, источники, резервный режим, редакторские статусы и подтверждение/отклонение материала.
- Мобильное меню, затемнение фона и закрытие касанием снаружи.

## Техническая проверка 2026-07-13

- `116 passed` в полном офлайн-наборе; LM Studio и сеть в диалоговых сценариях не используются.
- Ruff: без замечаний. Pyright: 0 ошибок и 0 предупреждений. `panel.js` проходит синтаксическую проверку Node.js.
- Восстановление плана после перезапуска проверено на заполненном плане из 7 элементов.
- На живом интерфейсе подтверждена отмена активной подготовки: кнопка меняется на «Отменить подготовку», появляется отдельное действие отмены, статус завершается как «подготовка плана отменена», все 7 прежних элементов остаются на месте.
- При выключенной LM Studio интерфейс показывает «LM Studio не запущена или недоступна», а состояние «запущена, но моделей нет» используется только после успешного ответа сервера со списком из нуля моделей.
- После сценариев отмены и проверки зависимостей в консоли браузера нет ошибок и предупреждений.
- Reference ASR работает в два прохода: быстрый `faster-whisper-small` и условный reviewer `faster-whisper-large-v3-turbo`, оба на CPU/int8 и с обязательной выгрузкой из памяти. Reviewer запускается только при расхождении, предупреждении уверенности или отсутствии ручного текста.
- На реальных WAV reviewer исправил имя «Зиана» на «Диана» и восстановил фразу Ирины. После нормализации `45` ↔ «сорок пять» и `чё` ↔ «что» совпадение обоих sidecar-текстов с reviewer составляет 100%; тёплый CPU-прогон занял 18,4 с и 14,5 с.
- Полный условный pipeline подтверждён: для спорной Ирины запустились оба прохода (31,5 с), для уже согласованного Максима reviewer был корректно пропущен (6,8 с). В обоих случаях сохранённый manual-текст подтверждён без изменений.
- Реальные reference WAV читаются корректно, но оба файла стерео; интерфейс показывает это как рекомендацию, а не скрывает качество исходника.
- OmniVoice прошёл реальный CUDA-синтез на RTX 5070 Ti: получен MP3 длительностью 3,50 с, 44,1 кГц, stereo. LM Studio в этом тесте не запускался.

## Responsive QA

Проверены **1487 × 1058**, **1024 × 768**, **820 × 900** и **390 × 844**. Горизонтального переполнения документа, плеера, инспектора и мобильных настроек нет. На узких экранах поля становятся одноколоночными, действия инспектора перестраиваются, а статус «Радио остановлено» остаётся полностью видимым.

## Закрытые дефекты

- P1: старые табличные настройки и несогласованные вкладки — заменены едиными карточками предпочтений.
- P1: плеер не занимал всю нижнюю границу — исправлено.
- P1: polling стирал редактируемый текст — исправлено.
- P1: проигранные элементы ошибочно показывались как будущие и редактируемые — исправлено.
- P1: мобильные поля обрезались — исправлено.
- P2: столкновения readiness, переполнение инспектора, фиктивные маркеры строк, несинхронные ARIA-состояния и нефункциональное сворачивание — исправлено.

Незакрытых P0/P1/P2 по финальному независимому review и browser QA нет.
